# Workspace Guidelines & Rules

## Tailwind CSS Rules

- Always use canonical Tailwind CSS v4 classes (e.g. `bg-linear-to-*` instead of `bg-gradient-to-*`, standard display utilities without redundant combinations like `block flex`, standard opacity modifiers).
